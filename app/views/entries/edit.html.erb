<div class="entrynew">

<%= form_for @entry, url: city_entry_path(@entry.city, @entry) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :body %>
  <%= f.submit "Save entry" %>
<% end %>

</div>